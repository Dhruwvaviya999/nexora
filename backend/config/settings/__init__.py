"""Settings package.

The active settings module is selected via the ``DJANGO_SETTINGS_MODULE``
environment variable, e.g.:

    config.settings.development   (default for local work)
    config.settings.production    (deploys)

See ``manage.py`` / ``wsgi.py`` / ``asgi.py`` for the default.
"""
